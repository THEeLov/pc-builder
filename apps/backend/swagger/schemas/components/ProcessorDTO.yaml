type: object
properties:
    architecture:
        type: string
    cores:
        type: integer
    threads:
        type: integer
    bits:
        type: integer
    socket:
        type: string
required:
    - architecture
    - cores
    - threads
    - bits
    - socket
