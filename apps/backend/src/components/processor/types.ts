export type Processor = {
    id: number
    name: string
    price: number
    manufacturer: string
    architecture: string
    cores: number
    threads: number
    bits: number
    socket: string
}
